import React from "react";
import { Link } from "react-router-dom";

const TopNavbar = () => {
  return (
    <div>
      <Link to="/mypage">마이페이지로 이동합니다.</Link>
    </div>
  );
};

export default TopNavbar;
